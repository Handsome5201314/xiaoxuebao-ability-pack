# Adapter Placeholders

This directory reserves integration points for runtimes that should consume the same public ability pack:

- `hermes`: primary runtime target for the first POC.
- `openclaw`: future channel and sandbox adapter.
- `fastapi`: future bridge to `xiaoxuebao-full`.
- `dify`: future export of workflow prompts, not the source of truth.
